[← Retour à l'index](README.md)

# Internationalisation (i18n)

**Sommaire**

- [Internationalisation (i18n)](#internationalisation-i18n)
  - [`LocalizedString` dans la configuration](#localizedstring-dans-la-configuration)
  - [Contexte de localisation](#contexte-de-localisation)
    - [Définition du contexte](#définition-du-contexte)
    - [Provider](#provider)
  - [Hook `useT`](#hook-uset)
  - [Exemple d'utilisation](#exemple-dutilisation)
  - [Cas particuliers : modules et composants hors configuration JSON](#cas-particuliers--modules-et-composants-hors-configuration-json)
    - [Chargement dynamique d'un namespace](#chargement-dynamique-dun-namespace)
    - [Organisation des fichiers i18n](#organisation-des-fichiers-i18n)
    - [Implémentation des hooks](#implémentation-des-hooks)
  - [Voir aussi](#voir-aussi)

---

La localisation de SiteForge repose principalement sur le type **`LocalizedString`** défini dans la configuration JSON et un contexte React dédié, complété par un hook `useT` pour rendre facilement les chaînes au component.

---

## `LocalizedString` dans la configuration

```ts
// src/types/locale-schema.ts
import { z } from "zod";

export const LOCALES = ["fr", "en", "es", "de"] as const; // extensible
export type Locale = (typeof LOCALES)[number];

export function createLocalizedStringSchema(defaultLang: Locale) {
  // 1) On génère dynamiquement un objet { fr: z.string().min(1).optional(), en: …, es: …, de: … }
  const shape = LOCALES.reduce((acc, locale) => {
    acc[locale] = z.string().min(1).optional();
    return acc;
  }, {} as Record<Locale, z.ZodOptional<z.ZodString>>);

  // 2) On en fait un objet Zod, qu'on rend "partiel", puis on raffine
  return z
    .object(shape)
    .partial()  // ici, chaque clé est maintenant facultative
    .refine(obj => Boolean(obj[defaultLang]), {
      message: `Le libellé doit inclure la langue par défaut (${defaultLang})`,
      path: [defaultLang],
    });
}

export const LocalizedString = createLocalizedStringSchema("fr");
export type LocalizedString = z.infer<typeof LocalizedString>;
```

* La fonction `createLocalizedStringSchema(defaultLang)` genere un schema Zod ou chaque locale est optionnelle, sauf la langue par defaut qui est requise via `refine()`.
* Toute valeur textuelle fournie par le site (titres, sous-titres, libelles...) utilise ce format :

  ```json
  {
    "headline": { "fr": "Bienvenue", "en": "Welcome" }
  }
  ```

---

## Contexte de localisation

### Définition du contexte

```tsx
// src/contexts/LocalizationContext.tsx
import { Locale, LocalizedString } from '@/types/locale-schema';
import { createContext } from 'react';

interface LocalizationContextType {
  currentLocale: Locale;
  setLocale: (locale: Locale) => void;
  t: (text: LocalizedString, fallback?: string) => string;
  availableLocales: readonly Locale[];
}

export const LocalizationContext = createContext<LocalizationContextType | null>(null);
```

Le contexte est **nullable** (`createContext<... | null>(null)`) -- les hooks qui le consomment doivent verifier sa presence.

### Provider

```tsx
// src/contexts/LocalizationProvider.tsx
import { type ReactNode, useState, useEffect } from 'react';
import { Locale, LOCALES, LocalizedString } from '@/types/locale-schema';
import { LocalizationContext } from './LocalizationContext';

interface LocalizationProviderProps {
  children: ReactNode;
  defaultLocale?: Locale;
  availableLocales?: readonly Locale[] | Locale[];
}

export function LocalizationProvider({
  children,
  defaultLocale = 'fr',
  availableLocales = LOCALES
}: LocalizationProviderProps) {
  const [currentLocale, setCurrentLocale] = useState<Locale>(defaultLocale);
  const [isClient, setIsClient] = useState(false);

  // Mark when we're on the client
  useEffect(() => {
    setIsClient(true);
  }, []);

  // Only access localStorage on the client
  useEffect(() => {
    if (!isClient) return;

    const savedLocale = localStorage.getItem('preferred-locale') as Locale;
    if (savedLocale && availableLocales.includes(savedLocale)) {
      setCurrentLocale(savedLocale);
    }
  }, [isClient, availableLocales]);

  const setLocale = (locale: Locale) => {
    setCurrentLocale(locale);
    if (typeof window !== 'undefined') {
      localStorage.setItem('preferred-locale', locale);
    }
  };

  const t = (text: LocalizedString, fallback = 'Missing translation'): string => {
    if (!text) return fallback;

    // Try current locale first
    if (text[currentLocale]) return text[currentLocale];

    // Try default locale
    if (text[defaultLocale]) return text[defaultLocale];

    // Try any available locale
    for (const locale of availableLocales) {
      if (text[locale]) return text[locale];
    }

    return fallback;
  };

  return (
    <LocalizationContext.Provider value={{
      currentLocale,
      setLocale,
      t,
      availableLocales
    }}>
      {children}
    </LocalizationContext.Provider>
  );
}
```

**Points cles** :
* **`defaultLocale`** et **`availableLocales`** : props configurables (par defaut `'fr'` et `LOCALES`).
* **`isClient`** : garde SSR -- `localStorage` n'est jamais accede cote serveur.
* **`preferred-locale`** : cle localStorage pour persister la locale choisie.
* **`currentLocale`** : code de la langue active (pas `locale`).
* **`t`** : fonction de traduction exposee dans le contexte, avec fallback en cascade (locale courante -> locale par defaut -> n'importe quelle locale -> fallback string).
* **`setLocale`** : change la locale et persiste dans `localStorage`.

---

## Hook `useT`

`useT` est un **hook hybride** qui gere a la fois les cles i18next (namespace) et les objets `LocalizedString` (configuration JSON) :

```ts
// src/hooks/useT.ts
import { useTranslation } from "react-i18next";
import { useLocalization } from "@/hooks/useLocalization";
import { LocalizedString } from "@/types/locale-schema";

export function useT(namespace?: string) {
  const { t: tNs } = useTranslation(namespace);
  const { t: tData } = useLocalization();

  return (key: string | LocalizedString, fallback?: string, interpolationParams?: Record<string, unknown>) => {
    if (typeof key === "string") {
      const options = {
        defaultValue: fallback || key,
        ...(interpolationParams || {})
      };
      const k = tNs(key, options);
      return k === key && fallback ? fallback : k;
    }
    return tData(key, fallback);
  };
}
```

* **Deux modes** :
  * Si `key` est un `string` : utilise `react-i18next` avec le namespace fourni (traductions JSON depuis `i18n/`)
  * Si `key` est un `LocalizedString` : utilise la fonction `t` du `LocalizationContext` (traductions de la config JSON)
* **`namespace`** (optionnel) : identifie le fichier de traduction i18next (ex: `"modules/profil"`)
* **`fallback`** et **`interpolationParams`** : parametres optionnels pour les traductions i18next

---

## Exemple d'utilisation

```tsx
import { useT } from "@/hooks/useT";

export function HeroSection({ props }) {
  const t = useT();
  return (
    <section>
      <h1>{t(props.headline)}</h1>
      {props.subhead && <p>{t(props.subhead)}</p>}
    </section>
  );
}
```

Chaque composant de section se sert ainsi de `useT()` pour afficher **toutes** les valeurs traduites.

---

## Cas particuliers : modules et composants hors configuration JSON

Certaines chaînes (dans les composants « layout », « ui », modules d'authentification, recherche, etc.) ne sont **pas** dans la config JSON et utilisent `react-i18next` de façon dynamique via :

* **`useLoadNamespace(namespace: string)`** : charge à la demande le fichier de traduction
* **`useT(namespace: string)`** : renvoie une fonction de traduction pour ce namespace

---

### Chargement dynamique d'un namespace

Dans n'importe quel composant :

```tsx
import { useLoadNamespace } from "@/hooks/useLoadNamespace";
import { useT } from "@/hooks/useT";

// Exemple pour le module d'authentification
export function LoginForm() {
  // Charge à la volée les JSON de traduction sous /components/auth/i18n
  const { loaded } = useLoadNamespace("components/auth");
  const t = useT("components/auth");

  if (!loaded) return <div>Chargement des traductions…</div>;

  return (
    <form>
      <label>{t("email_label")}</label>
      <input type="email" placeholder={t("email_placeholder")} />
      <button>{t("submit_button")}</button>
    </form>
  );
}
```

* **`useLoadNamespace("components/auth")`** :

  * Charge `src/components/auth/i18n/fr.json` et `en.json` via un import dynamique.
  * Renvoie `{ loaded: boolean }` pour indiquer quand la ressource est prête.

* **`useT("components/auth")`** :

  * Retourne une fonction `t(key: string): string` qui va chercher `key` dans le namespace chargé.
  * Exemple : `t("submit_button")` → `"Se connecter"` en français ou `"Login"` en anglais.

---

### Organisation des fichiers i18n

Chaque namespace correspond au **path** du dossier contenant le fichier `i18n` :

```
src/components/auth/i18n/fr.json
src/components/auth/i18n/en.json

src/modules/search/i18n/fr.json
src/modules/search/i18n/en.json

src/components/ui/button/i18n/fr.json
src/components/ui/button/i18n/en.json
```

Et ainsi de suite pour tous les composants/modules qui ont leurs propres libellés.

---

### Implémentation des hooks

* **`useLoadNamespace`** :

  ```ts
  // src/hooks/useLoadNamespace.tsx
  import { useTranslation } from "react-i18next";
  import { useEffect } from "react";

  const isServer = typeof window === "undefined";

  export function useLoadNamespace(ns: string) {
    const { i18n } = useTranslation();
    const loaded = i18n.hasResourceBundle(i18n.language, ns);

    useEffect(() => {
      if (!loaded) {
        i18n.loadNamespaces(ns);
      }
    }, [loaded, i18n, ns]);

    // En SSR, on considère toujours comme chargé pour éviter le mismatch d'hydration
    // Les traductions manquantes afficheront la clé, ce qui est acceptable
    if (isServer) {
      return { loaded: true };
    }

    return { loaded: i18n.hasResourceBundle(i18n.language, ns) };
  }
  ```

  **Points cles** :
  * Utilise `useTranslation()` pour obtenir l'instance `i18n` (et non un import direct)
  * Garde SSR : retourne `{ loaded: true }` cote serveur pour eviter les mismatches d'hydratation

* **`useT`** : voir la section [Hook `useT`](#hook-uset) ci-dessus pour l'implementation complete.

---

## Voir aussi

- [Schéma principal](04-schema-principal.md)
- [Configuration](02-configuration.md)
